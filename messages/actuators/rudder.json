{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "title": "Rudder",
    "description": "Actuator for given rudder control unit",
    "required": [
        "actuator_id",
        "angle_set"
    ],
    "properties": {
        "actuator_id": {
            "type": "string",
            "title": "Actuator id",
            "description": "The unique actuator id"
        },
        "angle_set": {
            "type": "number",
            "title": "Rudder angle set value",
            "description": "Rudder angle in degrees, positive when trailing edge points to port"
        }
    }
}
