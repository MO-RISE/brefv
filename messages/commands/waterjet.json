{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "title": "Waterjet command",
    "description": "Command given to a waterjet actuator",
    "required": [
        "command_id",
        "angle_set",
        "bucket_angle_set"
    ],
    "properties": {
        "command_id": {
            "type": "string",
            "title": "Command id",
            "description": "Platform unique actuators id"
        },
        "angle_set": {
            "type": "number",
            "title": "Waterjet angle set",
            "description": "Water-jet angle in degrees, positive when pushing water to port"
        },
        "bucket_angle_set": {
            "type": "number",
            "title": "Waterjet bucket angle set",
            "description": "Water-jet angle in degrees, positive when bucket letting water flow aft"
        }
    }
}