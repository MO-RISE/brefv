{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "title": "Rudder Steering",
    "description": "Command given for rudder steering unit",
    "required": [
        "command_id",
        "angle"
    ],
    "properties": {
        "actuator_id": {
            "type": "string",
            "title": "Actuator id",
            "description": "Platform unique actuators id"
        },
        "angle": {
            "type": "number",
            "title": "Rudder angle set",
            "description": "Rudder angle in degrees, positive when trailing edge points to port"
        }
    }
}