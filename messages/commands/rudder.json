{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "title": "Rudder command",
    "description": "Command given to a rudder actuator",
    "required": [
        "command_id",
        "angle_set"
    ],
    "properties": {
        "command_id": {
            "type": "string",
            "title": "Command id",
            "description": "Platform unique command id"
        },
        "angle_set": {
            "type": "number",
            "title": "Rudder angle set value",
            "description": "Rudder angle in degrees, positive when trailing edge points to port"
        },
        "flap_angle_set": {
            "type": "number",
            "title": "Rudder flap angle",
            "description": "Rudder flap angle in degrees, positive when trailing edge points to port"
        }
    }
}